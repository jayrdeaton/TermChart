# termchart

> **Deprecated** — this package has been folded into [termkit](https://www.npmjs.com/package/termkit). Please migrate to `termkit` instead.
>
> ```sh
> npm install termkit
> ```

A Node.js module for rendering charts (bar, line, scatter, heatmap, sparkline) in the terminal.
